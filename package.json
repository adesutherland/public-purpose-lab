{
  "name": "public-purpose-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.19.0"
  },
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm build:rust && pnpm build:web",
    "build:rust": "cargo build --workspace",
    "build:web": "pnpm --recursive --if-present build",
    "check": "pnpm check:architecture && pnpm check:contracts && pnpm check:links && pnpm format:check && pnpm check:web && pnpm check:rust && pnpm check:m1-runtime && pnpm check:m2-runtime",
    "check:architecture": "node tools/check-architecture-catalog.mjs",
    "check:contracts": "node tools/check-contracts.mjs",
    "check:links": "node tools/check-local-links.mjs",
    "check:m1-runtime": "node tools/check-m1-runtime.mjs",
    "check:m2-runtime": "node tools/check-m2-runtime.mjs",
    "check:rust": "cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "check:web": "pnpm --recursive --if-present typecheck && pnpm --recursive --if-present test",
    "dev:director": "pnpm --filter @public-purpose-lab/director dev",
    "dev:presentation": "pnpm --filter @public-purpose-lab/presentation dev",
    "dev:workbench": "pnpm --filter @public-purpose-lab/workbench dev",
    "format": "prettier --write .github/workflows/ci.yml architecture backend/README.md contracts deploy docs/architecture docs/programme/implementation-plan.md frontend tools DEVELOPMENT.md package.json pnpm-workspace.yaml",
    "format:check": "prettier --check .github/workflows/ci.yml architecture backend/README.md contracts deploy docs/architecture docs/programme/implementation-plan.md frontend tools DEVELOPMENT.md package.json pnpm-workspace.yaml",
    "test": "pnpm check"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "prettier": "3.9.6"
  }
}
